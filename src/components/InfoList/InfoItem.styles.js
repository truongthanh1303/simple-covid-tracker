import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(theme => ({
  count: {
    textAlign: 'center',
  },
  date: {
    marginTop: 12,
    textAlign: 'center'
  },
  time: {
    textAlign: 'center'
  },
  title: {
    textAlign: 'center'
  }
}));